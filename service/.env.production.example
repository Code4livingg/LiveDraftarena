# LiveDraft Arena Production Configuration
# Copy this file to .env and update values for your deployment

# Required: Application ID from Conway testnet deployment
LIVEDRAFT_APP_ID=your_deployed_application_id_here

# Production networking configuration
BIND_ADDRESS=0.0.0.0
PORT=8080

# Production CORS configuration (restrict to your frontend domains)
# Use comma-separated list for multiple domains
CORS_ORIGINS=https://your-frontend-domain.com,https://www.your-frontend-domain.com

# Production logging configuration
RUST_LOG=info
DEPLOYMENT_MODE=production

# Optional: Override default wallet path
# LINERA_WALLET_PATH=/path/to/your/wallet.json

# Optional: Override default chain ID (uses wallet default if not set)
# LIVEDRAFT_CHAIN_ID=your_lobby_chain_id_here

# Development mode example (for local testing):
# CORS_ORIGINS=*
# RUST_LOG=debug
# DEPLOYMENT_MODE=development